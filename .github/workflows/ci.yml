name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Setup / Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup / pnpm
        uses: pnpm/setup@fbda4c85fc2e1e08721cd8763afea8f48d60f024 # v3.0.0
        with:
          cache: true
          require-lockfile: true

      - name: Validate / Lint and Format
        run: pnpm check

  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - name: Setup / Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup / pnpm
        uses: pnpm/setup@fbda4c85fc2e1e08721cd8763afea8f48d60f024 # v3.0.0
        with:
          cache: true
          require-lockfile: true

      - name: Validate / Type Check
        run: pnpm typecheck

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Setup / Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup / pnpm
        uses: pnpm/setup@fbda4c85fc2e1e08721cd8763afea8f48d60f024 # v3.0.0
        with:
          cache: true
          require-lockfile: true

      # Valkey on 6379 and Redis on 6380, from the same compose.yaml the
      # Dev Container and local runs use.
      - name: Setup / Valkey and Redis
        run: docker compose up --detach --wait

      # With PNCH_REDIS_URL set, the integration tests fail instead of
      # skipping when the server is unreachable.
      - name: Validate / Test against Valkey
        run: pnpm test
        env:
          PNCH_REDIS_URL: redis://localhost:6379

      - name: Validate / Test against Redis
        run: pnpm test
        env:
          PNCH_REDIS_URL: redis://localhost:6380

  e2e:
    name: End-to-End Tests
    runs-on: ubuntu-latest
    steps:
      - name: Setup / Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup / pnpm
        uses: pnpm/setup@fbda4c85fc2e1e08721cd8763afea8f48d60f024 # v3.0.0
        with:
          cache: true
          require-lockfile: true

      # Valkey on 6379 and Redis on 6380, from the same compose.yaml the
      # Dev Container and local runs use.
      - name: Setup / Valkey and Redis
        run: docker compose up --detach --wait

      - name: Validate / End-to-end against Valkey
        run: pnpm test:e2e
        env:
          PNCH_REDIS_URL: redis://localhost:6379

      - name: Validate / End-to-end against Redis
        run: pnpm test:e2e
        env:
          PNCH_REDIS_URL: redis://localhost:6380

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Setup / Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup / pnpm
        uses: pnpm/setup@fbda4c85fc2e1e08721cd8763afea8f48d60f024 # v3.0.0
        with:
          cache: true
          require-lockfile: true

      - name: Validate / Build
        run: pnpm build
